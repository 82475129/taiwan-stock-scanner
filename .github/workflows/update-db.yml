name: 自動更新股票資料庫

on:
  schedule:
    - cron: '0 9 * * *' # 每天台灣時間 17:00 執行 (UTC 9:00)
  push:
    branches: [ main ] # 當你 push 程式碼時也會觸發
  workflow_dispatch:   # 允許你在 GitHub 介面手動點擊執行

jobs:
  update-process:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 賦予寫入權限，否則無法 push json 檔案
      
    steps:
    - name: 檢出代碼
      uses: actions/checkout@v4

    - name: 設定 Python 環境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 安裝必要套件
      run: |
        pip install requests beautifulsoup4

    - name: 執行資料庫更新程式
      run: |
        # 這裡執行你專門用來抓證交所資料的 update_db.py
        python update_db.py

    - name: 提交並推送更新後的 JSON
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add taiwan_full_market.json
        # 如果檔案沒有變動，commit 會失敗，所以加上 || exit 0
        git commit -m "🤖 自動更新台股清單 JSON" || exit 0
        git push
